{
  "name": "network-topology-visualizer",
  "version": "1.0.0",
  "description": "Monorepo for Network Topology Visualizer",
  "private": true,
  "workspaces": [
    "frontend",
    "ui-library"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix ui-library run build && npm --prefix frontend run build",
    "build:ui": "npm --prefix ui-library run build",
    "build:frontend": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "install:all": "npm install",
    "clean": "rmdir /s /q node_modules 2>nul & rmdir /s /q frontend\\node_modules 2>nul & rmdir /s /q ui-library\\node_modules 2>nul & del package-lock.json 2>nul & del frontend\\package-lock.json 2>nul & del ui-library\\package-lock.json 2>nul",
    "reinstall": "npm run clean && npm install"
  },
  "devDependencies": {}
}
