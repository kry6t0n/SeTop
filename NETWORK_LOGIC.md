# Network Connection Logic

## Типы соединений (Connection Types)

### 1. **Physical Connection (Физическое соединение)** 🔗
- **Цвет**: Синий
- **Стиль**: Сплошная линия
- **Когда используется**: Напрямую кабель от устройства к устройству
- **Совместимость**:
  - Router ↔ Switch: ✅
  - Router ↔ Workstation: ✅
  - Switch ↔ Server: ✅
  - Switch ↔ Workstation: ✅
  - Любой ↔ Network: ✅

### 2. **Routing Connection (Маршрутизация)** 🛣️
- **Цвет**: Фиолетовый
- **Стиль**: Пунктирная линия
- **Когда используется**: Router соединяет разные подсети
- **Совместимость**:
  - Router ↔ Router: ✅
  - Router ↔ Network: ✅

### 3. **Logical Connection (Логическое соединение)** 🔀
- **Цвет**: Зеленый
- **Стиль**: Пунктирная линия (двойная)
- **Когда используется**: Виртуальное соединение (VLAN, VPN)
- **Совместимость**: Любые устройства с IP адресом

## Правила валидации

### IP Сетевая совместимость
При соединении двух устройств (не через Router или Network):
- IP адреса должны быть в одной подсети
- Маска должна совпадать ИЛИ быть совместимой
- Иначе - требуется Router посредник

### Примеры совместимости IP

**Совместимо:**
- 192.168.1.1 (255.255.255.0) ↔ 192.168.1.2 (255.255.255.0) ✅
- 10.0.0.1 (255.255.0.0) ↔ 10.0.5.1 (255.255.0.0) ✅

**Несовместимо:**
- 192.168.1.1 (255.255.255.0) ↔ 192.168.2.1 (255.255.255.0) ❌
- Требуется Router!

## Структура Edge (соединения)

```javascript
{
  id: "edge-router1-switch1",
  source: "router-123",      // ID исходного узла
  target: "switch-456",      // ID целевого узла
  connectionType: "physical", // physical | routing | logical
  data: {
    bandwidth: "1Gbps",       // Опционально
    status: "active",         // active | inactive | error
    description: "Main link"  // Описание
  }
}
```

## Информация, которая будет отображаться в Edge Label

- Тип соединения (иконка)
- Статус (активно/неактивно)
- Пропускная способность (если указана)

## Логика при нажатии на соединение

1. Показать все детали в боковой панели (Toolbar)
2. Возможность:
   - Менять тип соединения
   - Менять статус
   - Удалить соединение
   - Менять описание
